# プロジェクト - spawn-qdrant

## 概要

このプロジェクトは、Dockerを使用してQdrantインスタンスを起動するためのものです。このプロジェクトはGoで記述されており、Qdrant Dockerイメージ
をラップするものです。このアプリケーションを使用すると、Linuxマシン上でDockerを使用して、qdrantのN個のインスタンスを起動できます。

## アプリケーションの使用方法

このアプリケーションは`spawn-qdrant`と呼ばれ、`.env`ファイルから次の引数を取ります。
- REST_PORT
- GRPC_PORT

`.env`ファイルが存在しない場合、アプリケーションはデフォルトの値を以下のように使用します。
- REST_PORT=6333
- GRPC_PORT=6334
（最初のインスタンスの起動ポート用）

アプリケーションの起動時に、DockerまたはPodmanがインストールされているかどうかを確認し、インストールされている場合はDockerを使用し、インスト
ールされていない場合はPodmanを使用します。どちらもインストールされていない場合は、エラーメッセージを表示し、DockerまたはPodmanのインストール
をユーザーに要求します。

### `check` サブコマンド

`check`コマンドは、システムRAMを確認し、起動時（各インスタンスあたり256MB）と効率的な運用（各インスタンスあたり512MB）の両方で実行できるイン
スタンスの数を報告します。

```bash
spawn-qdrant check
```

### `spawn` サブコマンド

起動するインスタンスの数を、`instance_count`というオプション引数として`spawn`サブコマンドに指定します。指定しない場合は、推定ロジック（
`check`と同様）が使用されます。

アプリケーションは、`qdrant/qdrant`イメージが存在するかどうかを確認し、存在しない場合はダウンロードします。

`spawn`サブコマンドは、`qdrant_network`という名前のDockerネットワークを作成し、すべてのコンテナをこのネットワークに接続します（すでに存在す
る場合は作成しません）。

例：

``` bash
spawn-qdrant spawn 2
```

これにより、以下のポートとストレージ場所を持つ2つのqdrantインスタンスが起動されます。

- コンテナ1：`qdrant-01`、ポート6333および6334、ネットワーク`qdrant_network`内のストレージ場所：`~/.qdrant_storage01`
- コンテナ2：`qdrant-02`、ポート6335および6336、ネットワーク`qdrant_network`内のストレージ場所：`~/.qdrant_storage02`

アプリケーションは、リソースの急激な変動を緩和するために、各インスタンスの起動の間に30秒の遅延を設けます。

ポートは、qdrantインスタンスの起動ポート番号です。実際のポートは、`REST_PORT + 2 * (instance_count - 1)`および`GRPC_PORT + 2 *
(instance_count - 1)`（`instance_count`が1の場合）になります。

各qdrantインスタンスのストレージは、`~/.qdrant_storage{instance_count}`に保存されます。

### `stop` サブコマンド

`stop`サブコマンドは、`spawn`サブコマンドを使用して起動されたすべてのqdrantインスタンスを停止します。このアプリケーションは、以下の方法で使
用できます。

#### `stop all` サブコマンド

このサブコマンドは、`spawn`サブコマンドを使用して起動されたすべてのqdrantインスタンスを停止し、`qdrant_network`とロックファイルを削除します
。

``` bash
spawn-qdrant stop all
```

#### `stop n` サブコマンド

このサブコマンドは、`spawn`サブコマンドを使用して起動されたn番目のqdrantインスタンスを停止します。また、これが最後のインスタンスの場合には、
`qdrant_network`とロックファイルを削除します。

``` bash
spawn-qdrant stop n
```

### `clean` サブコマンド

- このコマンドは、まず`spawn`サブコマンドを使用して起動されたqdrantインスタンスを停止します（内部的には`stop all`サブコマンドを使用）。
- アプリケーションは、`~/.qdrant_storage{instance_count}`のストレージ場所を、タイムスタンプ付きの単一のtar.gzファイルに圧縮し、
~/qdrant_backupという場所にバックアップします。
- アプリケーションは、すべての`spawn`サブコマンドを使用して起動されたqdrantインスタンスのストレージ場所`~/.qdrant_storage{instance_count}`を
削除します。
- gzipおよび削除コマンドは、Dockerコンテナとストレージ場所がrootによって所有されているため、sudo権限を使用してストレージ場所を処理します。

``` bash
spawn-qdrant clean
```

### ロックファイルメカニズム

このアプリケーションは、複数の同時`spawn`セッションを防止するために、`~/.spawn-qdrant.lock`という名前のロックファイルを使用します。
- **ロックの作成**: `spawn`を実行すると自動的に作成されます。
- **ロックの解除**: `stop all`、`clean`、または最後のインスタンスを停止した場合に自動的に削除されます。