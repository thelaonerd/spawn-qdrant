このドキュメントは、複数のコンテナインスタンスを生成し、管理するためのCLIアプリケーションのアーキテクチャテンプレートとして機能します。これ
により、堅牢なローカルのマルチコンテナオーケストレーションツールを構築するための、コアモジュール性とセキュリティの概念を抽象化できます。

## 1. 概要 / 抽象

この設計図は、複数のローカルコンテナをデプロイおよび管理するための汎用的なCLIツールについて記述しています。動的なポートマッピング、堅牢な同
時実行制御、およびシステムリソースの検証を使用することで、このアプリケーションは、n個のインスタンスを安全に処理できます。この設計は、コマン
ドラインインターフェース層と、実行時に依存しない内部抽象化を橋渡しする実行構造を採用しています。

## 2. アーキテクチャモジュール性 (ディレクトリ構造)

アプリケーションを2つの主要な層に分割することで、関心の分離を実現する必要があります。

### 2.1 コマンド層 (`cmd/`)
CLIのエントリーポイント (例: Cobraライブラリを使用)。この層は、入力を解析し、内部ビジネスロジックをオーケストレーションします。
*   **`root`**: グローバルなエントリーポイントとして機能します。競合状態を回避するために、ロックファイルの初期化などの設定操作を行います。
*   **`spawn` / `start`**: 必要なインスタンス数に対してホストのリソースを検証し、コンテナを順番に作成します。
*   **`check`**: システム要件 (例: 最小RAM/CPUの制限) を検証し、容量を報告します。
*   **`stop`**: 特定のコンテナインスタンスまたはすべての実行中のインスタンスを安全に停止します。
*   **`clean`**: 関連するストレージディレクトリまたはボリュームを安全にバックアップし、その後削除する破壊的なリセット操作を実行します。

### 2.2 内部抽象 (`internal/`)
この層は、ホストシステムおよびコンテナランタイムとのやり取りの具体的なメカニズムをカプセル化し、これらの詳細をCLI層から隠します。
*   **`container/`**: DockerまたはPodmanなどのツールをサポートする、コンテナ管理のためのCLI/API呼び出しを抽象化する、実行時非依存のラッパー
。
*   **`system/`**: ホストリソースの検出を担当します。たとえば、`/proc/meminfo`を読み取ったり、システムAPIを使用して利用可能なRAMとCPUの容量
を計算したりします。**互換性に関する注記**: このモジュールは、プラットフォーム固有の実装を通じて、異なるオペレーティングシステム (例:
Linux vs. macOS) を処理するように抽象化する必要があります。
*   **`lock/`**: ファイルベースのロックなどの、同時実行制御 (CLIの同時実行が、プロビジョニングまたは停止中に競合状態を引き起こさないように
) を実装します。
*   **`config/`**: 環境ベースの設定 (例: `.env`ファイル) を管理し、アプリケーション全体の設定を定義します。互換性を確保するために、アプリケ
ーション固有の詳細 (イメージ名、デフォルトポート、プレフィックス) をここで集約する必要があります。

## 3. コアオーケストレーションロジック

n個の同じコンテナを処理する場合、システムのスケーラビリティと予測可能なアクセスを確保するための特定のオーケストレーションロジックを適用する
必要があります。

*   **順次的なプロビジョニング**: コンテナの起動間隔に明示的な遅延 (例: 30秒の睡眠間隔) を導入することで、大規模な起動イベント中にホストシス
テムに負担がかかる可能性のあるCPUとI/Oのスパイクを軽減できます。
*   **リソース管理**: n個のコンテナを起動する前に、ホストのリソースのキャパシティを検証します。起動または実行中に、MB単位のRAMなどの最小リソ
ース要件を強制することで、OOM (Out of Memory) 障害を防ぎます。
*   **ネットワーク管理**: インスタンスを専用の共有ブリッジネットワーク (例: `app_network`) にアタッチし、コンテナ間の通信を可能にし、他のホ
ストサービスからの分離を実現します。
*   **動的なポートとボリュームのマッピング**: 1に基づくインスタンスのインデックス (iは1からnまで) に基づいて、ポートとストレージパスを決定す
るための公式な式が必要です。
    *   **接尾辞**: コンテナ名とストレージパスに0パディングされた接尾辞 (例: `01`、`02`、...、`10`) を使用して、一貫したソートと識別を確保し
ます。
    *   *例: ポートマッピング*: `PORT_A = Base_Port_A + Step * (i - 1)`
    *   *例: ボリュームマッピング*: `STORAGE_PATH = ~/.app_storage_0i`

## 4. セキュリティベストプラクティス

このツールは、ボリュームの削除やプロセスの管理などの特権操作を必要とします。特権昇格または誤ったデータの損失を防ぐために、以下のプラクティス
を適用する必要があります。

*   **シンボリックリンクの保護**: コンテナのストレージマウントのためのホストパスを動的に生成する場合、破壊的な操作を実行する前に、パスが実際
にディレクトリを指し、シンボリックリンクではないことを常に検証してください。 `os.Lstat`を使用してシンボリックリンクを検出します。
*   **非インタラクティブな安全性 (タイムアウト)**: バックグラウンドプロセス (例: `tar`によるバックアップまたは`rm`による削除) は、
`context.WithTimeout`を使用して厳密なコンテキストタイムアウト (例: 5〜10分) で実行する必要があります。これにより、`sudo`のパスワードが要求さ
れる可能性のある非インタラクティブまたはCI/CD環境で、CLIが無限にハングすることを防ぎます。
*   **ターミナル (TTY) の認識**: `sudo`のような、権威のある子プロセスを呼び出す場合、インタラクティブなターミナルが検出された場合にのみ、
`os.Stdin`をアタッチします。これにより、スクリプトが提供できない場合に待機し続けることを防ぎます。
*   **コマンドインジェクションの防止**: 変数またはユーザー定義されたパスを、システムコマンドに渡す際に、引数の区切り文字 (例: `--) を使用し
て、コマンドとして解釈されないようにします (例: `rm -rf -- /path/to/remove`)。